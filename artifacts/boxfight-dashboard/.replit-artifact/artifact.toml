kind = "web"
previewPath = "/"
title = "! boxfight auto ad"
version = "1.0.0"
id = "artifacts/boxfight-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24809

[services.development]
run = "pnpm --filter @workspace/boxfight-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/boxfight-dashboard", "run", "build" ]
publicDir = "artifacts/boxfight-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24809"
BASE_PATH = "/"
